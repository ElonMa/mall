spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://mysql.mayongfeng.cc:3306/mall_ums
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: mayongfeng.cc:8848
  application:
    name: mall-member


mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 8000